Goal:
  Provide human-perceivable E2E evidence (real device WebView, offline page).

Commands (PowerShell):
  .\gradlew :app:connectedAndroidTest --no-daemon
  pwsh -File .\scripts\pull-e2e-video.ps1

Expected artifacts:
  - Device frames (Downloads):
      /sdcard/Download/agent-browser-kotlin/e2e/frames/step-*.png
  - Local mp4 (built from frames):
      adb_dumps\e2e\latest\e2e-latest.mp4

Notes:
  - The instrumentation test pauses ~3.5s between steps for human observation.
  - The pull script requires ffmpeg in PATH.
